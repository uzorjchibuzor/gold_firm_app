<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Gold Firm Shs App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>
  <body>
    <div class="mx-auto px-5">
      <%= render "layouts/header" %>
      <% if notice %>
        <div class="bg-green-100 border border-green-400 text-green-800 px-4 py-3 rounded-md shadow-md">
          <div class="flex items-center justify-between">
            <span class="notice"><%= notice %></span>
          </div>
        </div>
      <% elsif alert %>
        <div class="bg-red-100 border border-red-400 text-red-800 px-4 py-3 rounded-md shadow-md">
          <div class="flex items-center justify-between">
            <span class="alert"><%= alert %></span>
          </div>
        </div>
      <% end %>
      <%= yield %>
      <%= render "layouts/footer"%>
    </div>
  </body>
</html>
